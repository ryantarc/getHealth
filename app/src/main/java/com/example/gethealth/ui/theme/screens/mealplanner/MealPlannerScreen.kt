package com.example.gethealth.ui.screens.mealplanner

import androidx.compose.runtime.Composable
import com.example.gethealth.ui.components.PlaceholderModuleScreen

/**
 * Meal Planner module — placeholder screen.
 *
 * This is where Student A will build the real meal planning feature
 * (meal lists, nutrition info, etc.). Everything they add should live in
 * this package (ui/screens/mealplanner) so their changes don't conflict
 * with the Fitness or Wellness modules on GitHub.
 */
@Composable
fun MealPlannerScreen() {
    PlaceholderModuleScreen(
        title = "Meal Planner",
        message = "This module will contain meal planning functionality."
    )
}

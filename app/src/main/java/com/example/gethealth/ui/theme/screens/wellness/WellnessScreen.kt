package com.example.gethealth.ui.screens.wellness

import androidx.compose.runtime.Composable
import com.example.gethealth.ui.components.PlaceholderModuleScreen

/**
 * Mood / Wellness module — placeholder screen.
 *
 * This is where Student C will build mood tracking and journal entries.
 * Keep new code inside ui/screens/wellness to avoid merge conflicts with
 * the other modules.
 */
@Composable
fun WellnessScreen() {
    PlaceholderModuleScreen(
        title = "Wellness",
        message = "This module will contain mood tracking and journaling."
    )
}

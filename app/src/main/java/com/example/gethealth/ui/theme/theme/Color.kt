package com.example.gethealth.ui.theme

import androidx.compose.ui.graphics.Color

/**
 * App color palette — matched to the GetHealth design deck (deep forest
 * green header/accents, warm off-white background, white cards, and
 * soft colored icon badges per module).
 */

// Core brand green (buttons, top bar, active states)
val ForestGreen = Color(0xFF1B4332)
val ForestGreenDark = Color(0xFF10281D)
val ForestGreenLight = Color(0xFF2D6A4F)

// Warm neutral background + card surface, matching the deck's light screens
val WarmBackground = Color(0xFFF7F5F0)
val CardSurface = Color(0xFFFFFFFF)
val MutedText = Color(0xFF6B7280)

// Soft badge colors used behind each module's icon on the dashboard
val BadgeGreenBg = Color(0xFFD9F2E3)
val BadgeGreenFg = Color(0xFF1B4332)

val BadgePurpleBg = Color(0xFFE9E1FB)
val BadgePurpleFg = Color(0xFF6D4FC4)

val BadgePeachBg = Color(0xFFFCE3D6)
val BadgePeachFg = Color(0xFFC4602F)
